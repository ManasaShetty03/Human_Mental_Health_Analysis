Flask==3.0.0
Flask-CORS==4.0.0
python-dotenv==1.0.0
Werkzeug==3.0.1
pymongo==4.6.0

# Build dependencies for Python 3.14 compatibility
setuptools>=75.0.0
wheel>=0.43.0

# Core dependencies for Python 3.14 compatibility
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0

# Audio Processing (compatible versions)
soundfile>=0.12.1

# Note: TensorFlow, librosa, xgboost, resampy removed for Python 3.14 compatibility
# These can be added later when Python 3.14 support is available
