services:
  - type: web
    name: mindcare-frontend
    runtime: static
    buildCommand: npm install && npm run build
    publishDir: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://mindcare-backend.onrender.com
      - key: VITE_GEMINI_API_KEY
        sync: false
